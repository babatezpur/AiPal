package com.saptarshi.aipal.ui.theme

import androidx.compose.ui.graphics.Color

// Primary — electric blue, the signature color. Used on buttons, FAB, active states.
val PrimaryBlue = Color(0xFF4D8DF7)
val PrimaryBlueLight = Color(0xFF80B0FF)
val PrimaryBlueDark = Color(0xFF1A5BC4)

// Accent — cyan spark. Used sparingly for highlights, toggles, special elements.
val AccentCyan = Color(0xFF00E5CC)

// Backgrounds — deep navy tones for dark mode feel even in light theme
val DarkNavy = Color(0xFF0D1B2A)
val DeepNavy = Color(0xFF1B2838)
val SurfaceNavy = Color(0xFF243447)

// Light mode backgrounds
val LightBackground = Color(0xFFF5F7FA)
val LightSurface = Color(0xFFFFFFFF)

// Text colors
val TextWhite = Color(0xFFECEFF4)
val TextGray = Color(0xFF8899AA)
val TextDark = Color(0xFF1A1E23)
val TextMuted = Color(0xFF6B7280)

// Status colors
val ErrorRed = Color(0xFFFF5B6A)
val SuccessGreen = Color(0xFF34D399)